#pragma once

enum INPUTKEY
{
	INPUTKEY_LEFT = 'a',
	INPUTKEY_RIGHT = 'd',
	INPUTKEY_UP = 'w',
	INPUTKEY_DOWN = 's',
	INPUTKEY_UNDO = 'n',
	INPUTKEY_OPTION = 'p',
	INPUTKEY_ESC = 27,
	INPUTKEY_ENTER = 13,
	INPUTKEY_SPACE = 32,
};

enum MARKLIST
{
	MARKLIST_FIRST = 1,
	MARKLIST_SECOND,
	MARKLIST_THIRD,
	MARKLIST_FOURTH,
	MARKLIST_END,
};

enum OPTIONS
{
	OPTIONS_SIZE = 1,
	OPTIONS_CURSOR,
	OPTIONS_STONE,
	OPTIONS_UNDO,
	OPTIONS_END,
};

enum UNDO
{
	UNDO_SET = 1,
	UNDO_OFF,
	UNDO_END
};

enum UNDOCOUNT
{
	UNDOCOUNT_MIN = 0,
	UNDOCOUNT_MAX = 10
};

enum MAPRANGE
{
	MAPRANGE_WIDTH_MIN = 20,
	MAPRANGE_WIDTH_MAX = 90,
	MAPRANGE_HEIGHT_MIN = 20,
	MAPRANGE_HEIGHT_MAX = 45,
};

enum MAINMENU
{
	MAINMENU_START_GAME = 1,
	MAINMENU_REPLAY,
	MAINMENU_OPTIONS,
	MAINMENU_CLOSE
};

enum PLAYER
{
	PLAYER_1P = 0,
	PLAYER_2P = 1,
};

enum DIR
{
	DIR_START,
	DIR_UP,
	DIR_DOWN,
	DIR_LEFT,
	DIR_RIGHT,
	DIR_UPLEFT,
	DIR_DOWNRIGHT,
	DIR_UPRIGHT,
	DIR_DOWNLEFT
};

enum PLAYMODE
{
	PLAYMODE_NEW,
	PLAYMODE_CONTINUE
};